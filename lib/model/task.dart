class Task {
  // A priori, en dart les champs n'ont pas besoin d ete prives comme les conventions de java
  String title;
  String description;

  Task({
    required this.title,
    required this.description,
  });
}
